import * as Actions from "../actions/actionDefinitions";

const initialState = {
  ruleset: "CHARGES"
}

export default (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
}